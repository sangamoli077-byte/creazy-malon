export interface RoadmapItem {
  phase: string;
  title: string;
  items: string[];
  color: string;
}

export interface SocialLink {
  name: string;
  url: string;
  icon: string;
}

export interface TokenomicsData {
  name: string;
  value: number;
  fill: string;
}